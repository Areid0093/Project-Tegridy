<%= tag.h1 'Advanced Job Search' %>
<%= tag.header link_to_toggle_search_modes, class:"btn btn-link btn-lg btn-block" %>

<%= search_form_for(
    @search,
    url: advanced_search_job_postings_path,
    html: { method: :post }
  ) do |f| %>

  <% setup_search_form(f) %>

  <%= tag.fieldset do %>
    <%= tag.legend 'Sorting' %>
    <%= f.sort_fields do |s| %>
      <%= render 'sort_fields', f: s %>
    <% end %>
    <%= button_to_add_fields(f, :sort) %>
  <% end %>

  <%= tag.fieldset do %>
    <%= tag.legend 'Condition Groups' %>
    <%= f.grouping_fields do |g| %>
      <%= render 'grouping_fields', f: g %>
    <% end %>
    <%= button_to_add_fields(f, :grouping) %>
  <% end %>

  <%= display_distinct_label_and_check_box %>
  <%= f.submit class:"btn btn-outline-dark" %>
<% end %> <br>

<%= render 'results' %>