<%= form_for @bookmark do |f| %>
  <div class="field">
 <%# <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.collection_select :job_posting_id, JobPosting.all, :id, :company %>

</div>

  <div class="actions">
    <%= f.submit "Add to Boomkmarks" %>
  </div>
<% end %>

