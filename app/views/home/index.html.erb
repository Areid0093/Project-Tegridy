<% if !current_user %>

  
<div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column text-center">
      
      <main role="main" class="inner cover">
        <h1 class="cover-heading">Welcome to Tegridy</h1>
        <p class="lead">You can't spell Integrity without Tegridy.</p>
        <p class="lead">
          <a href=<%= signup_path %> class="btn btn-lg btn-info">Sign up</a>
          <a href=<%= login_path %> class="btn btn-lg btn-info">Log in</a>
        </p>
      </main>
</div>

<% end %>
